# Resume Studio reference (not a second app)

This folder is the integration kit shipped as `ApplyOS-ResumeStudio-v1.0.0`. The live implementation lives in `src/lib/resume/` and the existing Apply OS opportunity UI.

Do not run this folder as its own application. Do not copy its `package.json` over the repo root.
