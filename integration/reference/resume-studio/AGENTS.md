# Integration reference, not a replacement app

Read README.md and CURSOR_INTEGRATION_PROMPT.md. Source of truth: the existing validated Apply OS profile, never this kit's fixtures or historical uploads.

Keep all provider, evidence and approval operations server-side. No application submission. Do not modify candidate facts to improve a score. Keep source IDs and qualifiers. Run the supplied checks after adaptation. Never report live integration or export validation as passed unless actually executed.
